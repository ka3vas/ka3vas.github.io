import defaultSet from '../set/default';
import defaultPoke from '../poke/default';
import defaultRoll from '../roll/default';
import defaultTempura from '../tempura/default';

const kwidzynDishes = [
  ...defaultSet,
  ...defaultPoke,
  ...defaultRoll,
  ...defaultTempura,
];

export default kwidzynDishes;
